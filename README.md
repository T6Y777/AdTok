# AdTok

把抖音 / B 站网页版伪装成右下角广告弹窗的摸鱼神器。

## 功能

- 右下角弹窗样式，标题栏显示"热门推荐"，外观酷似广告弹窗
- 内嵌浏览器加载抖音网页版，支持登录、刷视频
- 全局老板键 `Ctrl + `` 一键显示/隐藏
- 关闭/最小化都隐藏到系统托盘，不退出程序
- 窗口位置和大小自动记忆
- 登录状态持久化保存

## 快速开始

### 1. 安装依赖（首次运行）

```bat
.venv\Scripts\pip install -r requirements.txt
```

### 2. 启动

双击 `run.bat`，或命令行执行：

```bat
.venv\Scripts\python main.py
```

## 使用说明

| 操作 | 效果 |
|------|------|
| `Ctrl + `` | 显示 / 隐藏弹窗 |
| 左键单击托盘图标 | 显示 / 隐藏弹窗 |
| 右键托盘图标 | 菜单（显示/隐藏、退出） |
| 拖拽标题栏 | 移动窗口 |
| 点击 ✕ / — | 隐藏到托盘 |

## 切换到 B 站

在弹窗内的浏览器地址栏输入 `https://www.bilibili.com` 即可（后续版本会加平台切换按钮）。

## 项目结构

```
AdTok/
├── main.py           # 程序入口
├── config.py         # 配置管理（持久化）
├── hotkey.py         # Windows 全局热键
├── popup_window.py   # 弹窗主窗口 + 自定义标题栏
├── requirements.txt  # 依赖
├── run.bat           # 双击启动脚本
└── tests/            # 测试
```
